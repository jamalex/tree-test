from django.conf.urls import patterns, include, url

from tree.views import current_datetime,hello

from django.contrib import admin
admin.autodiscover()

urlpatterns = patterns('',
    # Examples:
    # url(r'^$', 'tree.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),

    url(r'^admin/', include(admin.site.urls)),
    url(r'^template/$', current_datetime),
    url(r'^hello/$', hello),
    
    #url(r'^load/$', load , {'addr' : ' ' }),
    #url(r'^load/stories-and-literature/$', load , {'addr' : 'stories-and-literature'} ),
    #url(r'^load/stories-and-literature/fairy-tales/$', load , {'addr' : 'fairy-tales'} ),
    #url(r'^load/stories-and-literature/fairy-tales/little-red-riding-hood/$', load , {'addr' : 'little-red-riding-hood'}),
    #url(r'^load/stories-and-literature/fairy-tales/boy-who-cried-wolf/$', load , {'addr' : 'boy-who-cried-wolf'}),
    #url(r'^load/stories-and-literature/fairy-tales/emperors-new-clothes/$', load , {'addr' : 'emperors-new-clothes'}),
    #url(r'^load/stories-and-literature/classic-literature/$', load , {'addr' : 'classic-literature'}),
)
